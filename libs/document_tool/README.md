# Fetches documents from different wiki-like sources

## Implementations
- Confluence

## Known issues
Confluence seems to have a bug where if you add in the `creator` filter, it always returns 0 results. Removing it returns results.